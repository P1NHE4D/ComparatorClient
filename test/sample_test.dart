import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:comparator/main.dart';

void main() {
  testWidgets('Sample test', (WidgetTester tester) async {
    expect(1, 1);
  });
}
